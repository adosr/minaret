# Customization

To adapt this repository for another app in the future:

1. Replace all `minaret-*` files inside domain-specific folders.
2. Update branding assets in `apps/web/img`.
3. Update `packages/core/app-config.js`.
4. Update translation strings in `apps/web/locales`.
