# Minaret

Monorepo structure for the Minaret web app and push worker.

- `apps/web` — static PWA frontend
- `apps/worker` — Cloudflare Worker for web push
- `packages/shared` — domain logic shared by frontend and worker
- `packages/core` — shared configuration
- `packages/ui` — UI tokens and layout helpers
